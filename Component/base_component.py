class DashboardComponent:
    def render(self):
        """
        Render the component in the Streamlit dashboard.
        This method should be overridden by all derived components.
        """
        raise NotImplementedError("Subclasses must implement the render method.")
